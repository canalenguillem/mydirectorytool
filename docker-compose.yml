services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
      - ./backend/.env.auth
    volumes:
      - ./data:/data
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    ports:
      - "8091:8091"
    depends_on:
      - backend
    restart: unless-stopped
